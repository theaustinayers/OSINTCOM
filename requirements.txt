# OSINTCOM dependencies
sounddevice
numpy
scipy
pyaudio
requests
python-dotenv
# For WASAPI loopback (Windows only)
pycaw; platform_system == "Windows"
# For denoising (optional, basic)
noisereduce
# For ML-based speech detection (pre-trained, lightweight, offline)
silero-vad
